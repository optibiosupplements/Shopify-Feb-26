{% comment %}
  ═══════════════════════════════════════════════════════════════
  Optibio Hero Section — Design D: Editorial
  Magazine-style layout: editorial narrative left, product card right
  "Warm authority" tone: clinical but human, not robotic
  ═══════════════════════════════════════════════════════════════

  DEPLOYMENT: Replace the existing sections/homepage-hero.liquid
  with this file. CSS is self-contained in <style> tags.

  SETTINGS: All text, images, and CTA links are editable from
  the Shopify theme customizer (Customize > Homepage > Hero).
{% endcomment %}

{{ 'homepage-hero-editorial.css' | asset_url | stylesheet_tag }}

<section
  id="optibio-hero"
  class="optibio-hero-ed"
  data-section-id="{{ section.id }}"
  data-section-type="homepage-hero"
>
  {%- comment -%} ── HERO GRID: Editorial Left + Product Card Right ── {%- endcomment -%}
  <div class="optibio-hero-ed__container">

    {%- comment -%} ══ LEFT COLUMN: Editorial Content ══ {%- endcomment -%}
    <div class="optibio-hero-ed__editorial">

      {%- comment -%} Category Tag ─────────────────────── {%- endcomment -%}
      <div class="optibio-hero-ed__category">
        {{ section.settings.category_text | default: 'Adaptogens & Wellness' }}
      </div>

      {%- comment -%} Headline ─────────────────────────── {%- endcomment -%}
      <h1 class="optibio-hero-ed__headline">
        {{ section.settings.headline_line1 | default: 'Your Body' }}<br>
        {{ section.settings.headline_line2 | default: 'Knows How' }}<br>
        {{ section.settings.headline_line3 | default: 'to' }}
        <em>{{ section.settings.headline_accent | default: 'Thrive.' }}</em>
      </h1>

      {%- comment -%} Lede (italic intro paragraph) ───── {%- endcomment -%}
      <p class="optibio-hero-ed__lede">
        {{ section.settings.lede_text | default: 'Sometimes it just needs the right support. KSM-66® Ashwagandha is the most clinically studied adaptogen in the world — and we deliver the full clinical dose.' }}
      </p>

      {%- comment -%} Body Copy ─────────────────────────── {%- endcomment -%}
      <p class="optibio-hero-ed__body">
        {{ section.settings.body_text | default: "Stress doesn't announce itself. It accumulates — in tight shoulders, restless nights, and that low hum of exhaustion you've learned to live with. KSM-66 works with your body's own stress-response system, helping you find balance again." }}
      </p>

      {%- comment -%} Pull Quote ─────────────────────────── {%- endcomment -%}
      {%- if section.settings.show_pull_quote -%}
        <blockquote class="optibio-hero-ed__pull-quote">
          <p>&ldquo;{{ section.settings.quote_text | default: "The difference between feeling like you're surviving and feeling like you're actually living." }}&rdquo;</p>
          <cite>{{ section.settings.quote_attribution | default: 'What our customers tell us' }}</cite>
        </blockquote>
      {%- endif -%}

      {%- comment -%} CTAs ─────────────────────────────── {%- endcomment -%}
      <div class="optibio-hero-ed__ctas">
        <a href="{{ section.settings.primary_cta_link | default: '/products/optibio-ashwagandha-ksm-66' }}" class="optibio-hero-ed__btn-gold">
          {{ section.settings.primary_cta_text | default: 'Start Feeling Better' }}
        </a>
        <a href="{{ section.settings.secondary_cta_link | default: '/pages/science' }}" class="optibio-hero-ed__btn-outline">
          {{ section.settings.secondary_cta_text | default: 'Read the Research' }}
        </a>
      </div>

      {%- comment -%} Trust Row ─────────────────────────── {%- endcomment -%}
      <div class="optibio-hero-ed__trust-row">
        {%- for block in section.blocks -%}
          {%- if block.type == 'trust_item' -%}
            <div class="optibio-hero-ed__trust-item" {{ block.shopify_attributes }}>
              <div class="optibio-hero-ed__trust-icon">
                {{ block.settings.icon | default: '✓' }}
              </div>
              <div class="optibio-hero-ed__trust-text">
                <strong>{{ block.settings.title | default: 'Clinically Studied' }}</strong>
                {{ block.settings.subtitle | default: '24 peer-reviewed trials' }}
              </div>
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>

      {%- comment -%} Detail Grid (hybrid from Design B) ── {%- endcomment -%}
      <div class="optibio-hero-ed__detail-grid">
        {%- for block in section.blocks -%}
          {%- if block.type == 'detail_item' -%}
            <div class="optibio-hero-ed__detail-item" {{ block.shopify_attributes }}>
              <strong>{{ block.settings.label | default: 'Dose' }}</strong>
              {{ block.settings.value | default: '600mg full-spectrum root extract' }}
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>

    </div>

    {%- comment -%} ══ RIGHT COLUMN: Product Feature Card ══ {%- endcomment -%}
    <div class="optibio-hero-ed__feature-card">

      <div class="optibio-hero-ed__feature-tag">
        {{ section.settings.feature_tag | default: 'Featured Product' }}
      </div>

      {%- comment -%} Product Image ─────────────────────── {%- endcomment -%}
      {%- comment -%} Priority: 1) Manual image picker  2) Product featured image  3) CSS placeholder {%- endcomment -%}
      {%- assign hero_handle = section.settings.product_link | split: '/products/' | last -%}
      {%- assign hero_product = all_products[hero_handle] -%}
      <div class="optibio-hero-ed__product-image">
        {%- if section.settings.product_image != blank -%}
          <img
            src="{{ section.settings.product_image | image_url: width: 500 }}"
            alt="{{ section.settings.product_image.alt | default: 'Optibio Ashwagandha KSM-66®' }}"
            width="500"
            height="auto"
            loading="eager"
            fetchpriority="high"
          >
        {%- elsif hero_product != blank and hero_product.featured_image != blank -%}
          <img
            src="{{ hero_product.featured_image | image_url: width: 500 }}"
            alt="{{ hero_product.featured_image.alt | default: hero_product.title | default: 'Optibio Ashwagandha KSM-66®' }}"
            width="500"
            height="auto"
            loading="eager"
            fetchpriority="high"
          >
        {%- else -%}
          {%- comment -%} CSS-only bottle placeholder ─── {%- endcomment -%}
          <div class="optibio-hero-ed__bottle-placeholder">
            <div class="optibio-hero-ed__bottle-cap"></div>
            <div class="optibio-hero-ed__bottle-body">
              <div class="optibio-hero-ed__bottle-label">
                <span class="optibio-hero-ed__bl-brand">OPTIBIO</span><!-- All-caps in label is visual only -->
                <span class="optibio-hero-ed__bl-line"></span>
                <span class="optibio-hero-ed__bl-name">Ashwagandha<br>KSM-66&reg;</span>
                <span class="optibio-hero-ed__bl-dose">600 MG</span>
                <span class="optibio-hero-ed__bl-type">Full-Spectrum Root Extract</span>
              </div>
            </div>
          </div>
        {%- endif -%}
      </div>

      {%- comment -%} Price ────────────────────────────── {%- endcomment -%}
      <div class="optibio-hero-ed__feature-price">
        <span class="optibio-hero-ed__fp-current">{{ section.settings.price | default: '$39.99' }}</span>
        {%- if section.settings.compare_price != blank -%}
          <span class="optibio-hero-ed__fp-compare">{{ section.settings.compare_price }}</span>
        {%- endif -%}
        <span class="optibio-hero-ed__fp-detail">{{ section.settings.price_detail | default: '90 Capsules · 45-Day Supply' }}</span>
      </div>

      {%- comment -%} Add to Cart Button ────────────────── {%- endcomment -%}
      <a href="{{ section.settings.product_link | default: '/products/optibio-ashwagandha-ksm-66' }}" class="optibio-hero-ed__feature-btn">
        {{ section.settings.feature_cta_text | default: 'Add to Bag' }}
      </a>

      {%- comment -%} Stat Highlights ─────────────────── {%- endcomment -%}
      <div class="optibio-hero-ed__feature-stats">
        <div class="optibio-hero-ed__stat">
          <strong>24</strong>
          Clinical Studies
        </div>
        <div class="optibio-hero-ed__stat">
          <strong>600</strong>
          mg per dose
        </div>
        <div class="optibio-hero-ed__stat">
          <strong>90</strong>
          Day Guarantee
        </div>
      </div>

    </div>
  </div>

  {%- comment -%} ── SCIENCE STRIP (below hero) ── {%- endcomment -%}
  {%- if section.settings.show_science_strip -%}
    <div class="optibio-hero-ed__science-strip">
      <div class="optibio-hero-ed__sci-item">
        <span class="optibio-hero-ed__sci-number">24</span>
        <span class="optibio-hero-ed__sci-label">Peer-Reviewed Studies</span>
      </div>
      <div class="optibio-hero-ed__sci-divider"></div>
      <div class="optibio-hero-ed__sci-item">
        <span class="optibio-hero-ed__sci-number">600mg</span>
        <span class="optibio-hero-ed__sci-label">Full Clinical Dose</span>
      </div>
      <div class="optibio-hero-ed__sci-divider"></div>
      <div class="optibio-hero-ed__sci-item">
        <span class="optibio-hero-ed__sci-number">5:1</span>
        <span class="optibio-hero-ed__sci-label">Root Extract Concentration</span>
      </div>
      <div class="optibio-hero-ed__sci-divider"></div>
      <div class="optibio-hero-ed__sci-item">
        <span class="optibio-hero-ed__sci-number">GMP</span>
        <span class="optibio-hero-ed__sci-label">Certified Facility</span>
      </div>
    </div>
  {%- endif -%}

  {%- comment -%} ── FDA DISCLAIMER ── {%- endcomment -%}
  <p class="optibio-hero-ed__fda">
    *These statements have not been evaluated by the Food and Drug Administration.
    This product is not intended to diagnose, treat, cure, or prevent any disease.
    Individual results may vary.
  </p>

</section>

{% schema %}
{
  "name": "Hero — Editorial",
  "tag": "section",
  "class": "optibio-hero-wrapper",
  "settings": [
    {
      "type": "header",
      "content": "Category & Headline"
    },
    {
      "type": "text",
      "id": "category_text",
      "label": "Category tag",
      "default": "Adaptogens & Wellness"
    },
    {
      "type": "text",
      "id": "headline_line1",
      "label": "Headline line 1",
      "default": "Your Body"
    },
    {
      "type": "text",
      "id": "headline_line2",
      "label": "Headline line 2",
      "default": "Knows How"
    },
    {
      "type": "text",
      "id": "headline_line3",
      "label": "Headline line 3 (before accent)",
      "default": "to"
    },
    {
      "type": "text",
      "id": "headline_accent",
      "label": "Headline accent word (italic gold)",
      "default": "Thrive."
    },
    {
      "type": "header",
      "content": "Body Copy"
    },
    {
      "type": "textarea",
      "id": "lede_text",
      "label": "Lede paragraph (italic intro)",
      "default": "Sometimes it just needs the right support. KSM-66® Ashwagandha is the most clinically studied adaptogen in the world — and we deliver the full clinical dose."
    },
    {
      "type": "textarea",
      "id": "body_text",
      "label": "Body paragraph",
      "default": "Stress doesn't announce itself. It accumulates — in tight shoulders, restless nights, and that low hum of exhaustion you've learned to live with. KSM-66 works with your body's own stress-response system, helping you find balance again."
    },
    {
      "type": "header",
      "content": "Pull Quote"
    },
    {
      "type": "checkbox",
      "id": "show_pull_quote",
      "label": "Show pull quote",
      "default": true
    },
    {
      "type": "textarea",
      "id": "quote_text",
      "label": "Quote text",
      "default": "The difference between feeling like you're surviving and feeling like you're actually living."
    },
    {
      "type": "text",
      "id": "quote_attribution",
      "label": "Quote attribution",
      "default": "What our customers tell us"
    },
    {
      "type": "header",
      "content": "Call-to-Action Buttons"
    },
    {
      "type": "text",
      "id": "primary_cta_text",
      "label": "Primary CTA text",
      "default": "Start Feeling Better"
    },
    {
      "type": "url",
      "id": "primary_cta_link",
      "label": "Primary CTA link"
    },
    {
      "type": "text",
      "id": "secondary_cta_text",
      "label": "Secondary CTA text",
      "default": "Read the Research"
    },
    {
      "type": "url",
      "id": "secondary_cta_link",
      "label": "Secondary CTA link"
    },
    {
      "type": "header",
      "content": "Product Feature Card"
    },
    {
      "type": "image_picker",
      "id": "product_image",
      "label": "Product image"
    },
    {
      "type": "text",
      "id": "feature_tag",
      "label": "Feature card tag",
      "default": "Featured Product"
    },
    {
      "type": "text",
      "id": "price",
      "label": "Display price",
      "default": "$39.99"
    },
    {
      "type": "text",
      "id": "compare_price",
      "label": "Compare-at price (leave blank to hide)",
      "default": "$49.99"
    },
    {
      "type": "text",
      "id": "price_detail",
      "label": "Price detail text",
      "default": "90 Capsules · 45-Day Supply"
    },
    {
      "type": "text",
      "id": "feature_cta_text",
      "label": "Feature card CTA text",
      "default": "Add to Bag"
    },
    {
      "type": "url",
      "id": "product_link",
      "label": "Product link"
    },
    {
      "type": "header",
      "content": "Science Strip"
    },
    {
      "type": "checkbox",
      "id": "show_science_strip",
      "label": "Show science numbers strip",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "trust_item",
      "name": "Trust Badge",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon character",
          "default": "✓"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Clinically Studied"
        },
        {
          "type": "text",
          "id": "subtitle",
          "label": "Subtitle",
          "default": "24 peer-reviewed trials"
        }
      ]
    },
    {
      "type": "detail_item",
      "name": "Detail Item",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Dose"
        },
        {
          "type": "text",
          "id": "value",
          "label": "Value",
          "default": "600mg full-spectrum root extract"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero — Editorial",
      "blocks": [
        {
          "type": "trust_item",
          "settings": {
            "icon": "✓",
            "title": "Clinically Studied",
            "subtitle": "24 peer-reviewed trials"
          }
        },
        {
          "type": "trust_item",
          "settings": {
            "icon": "◈",
            "title": "Full Clinical Dose",
            "subtitle": "600mg KSM-66®"
          }
        },
        {
          "type": "trust_item",
          "settings": {
            "icon": "☆",
            "title": "90-Day Guarantee",
            "subtitle": "No questions asked"
          }
        },
        {
          "type": "detail_item",
          "settings": {
            "label": "Dose",
            "value": "600mg full-spectrum root extract"
          }
        },
        {
          "type": "detail_item",
          "settings": {
            "label": "Studies",
            "value": "24 peer-reviewed clinical trials"
          }
        },
        {
          "type": "detail_item",
          "settings": {
            "label": "Standard",
            "value": "Third-party tested, GMP certified"
          }
        }
      ]
    }
  ]
}
{% endschema %}
